# Phase 2 — Dual-model scoring pipeline
# Install with: pip install -r requirements.txt

# PDF extraction (S02)
pdfplumber>=0.10.0
pytesseract>=0.3.10        # requires tesseract-ocr system package

# Near-duplicate detection (S04)
datasketch>=1.0.7

# LLM scoring (S06_GPT and S06_CLAUDE)
openai>=1.30.0
anthropic>=0.28.0

# Config and data models
pyyaml>=6.0
pydantic>=2.0.0

# Excel export (jsonl_to_xlsx_v2.py only)
pandas>=2.0.0
xlsxwriter>=3.1.0
